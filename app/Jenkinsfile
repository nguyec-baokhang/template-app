pipeline{
    agent any 
    stages{
        stage('Build'){
            steps{
                sh '''
                    docker build -f app/Dockerfile -t template_app . 
                '''
            }
        }
        stage('Run'){
            steps{
                sh'''
                    docker run -d template_app -p 8000:8000
                '''
            }
        }
    }
}