# helm/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "templateapp.fullname" . }}
  labels:
    {{- include "templateapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "templateapp.selectorLabels" . | nindent 4 }}